#ifndef __TYPES__
#define __TYPES__

typedef enum {false, true} bool;

typedef enum {
    COMMAND_ERR

} ERROR;

typedef enum {
    COMMAND_WARNING

} WARNING;

#define PAGE_SIZE (4*1024) // 4K

#endif
